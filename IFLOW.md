# Yigo-Edu-Agent 项目说明

## 项目概述

Yigo-Edu-Agent 是一个基于 Microsoft AutoGen 框架构建的面向教育场景的多智能体仿真平台。该项目旨在通过模拟不同的教学和讨论模式，为未来的教育应用提供原型和技术验证。

### 核心目标
- **讨论仿真**：模拟学生群体的讨论互动
- **教学授课仿真**：为师范生提供虚拟教学与课堂管理的训练环境

### 技术栈
- **框架**：Microsoft AutoGen (v0.7.5)
- **语言**：Python 3.x
- **LLM**：OpenAI API (通过环境变量配置)
- **依赖管理**：pip + requirements.txt

### 项目架构设计
项目计划采用 Django 后端架构，包含以下核心模块：
- `agents`：智能体管理（人格、记忆、知识状态）
- `dialogues`：对话与交互管理
- `interventions`：干预与反馈模块
- `analytics`：数据分析与评估
- `tasks`：任务与场景配置
- `logs`：日志与数据存储
- `api`：REST API / WebSocket 接口

## 当前实现状态

### 核心文件

#### 1. `test_manager_ver.py` - 中心化群聊模式（主要实现）
这是当前项目的核心成果，展示了通过继承和方法重写对 AutoGen 进行深度定制的能力。

**核心特性：**
- **EnhancedClassroomChat 类**：继承自 `autogen.GroupChat`，重写了 `select_speaker` 方法
- **情景感知调度**：识别老师话语中的"开始讨论"信号，自动切换到学生间的自由讨论模式
- **多维决策逻辑**：综合运用点名检测、老师提问、学生间互动、发言沉默轮数等多种规则
- **状态化管理**：引入"讨论模式"标记、轮数计数器、学生沉默计数器等状态
- **异构智能体**：通过精心设计的 `system_message`，塑造性格各异的学生（积极型、严谨型、批判型）

**智能体配置：**
- `Teacher`：物理老师，引导讨论、提问、总结
- `StudentA`：积极但知识不深的学生，口语化表达
- `StudentB`：严谨、知识较好的学生，回答准确
- `StudentC`：爱质疑、批判性思维的学生，喜欢提反例
- `Coordinator`：协调员，负责启动和终止讨论

#### 2. `test_p2p_ver.py` - 点对点对话模式
演示两个智能体之间直接进行对话的模式，适用于一对一辅导、问答、任务委托等场景。

#### 3. `knowledge_graph.py` - 动态知识图谱模块（2025-12-28 新增）
实现第一个创新方向的核心模块，为智能体提供知识状态的显式建模能力。

**核心功能：**
- **Concept 类**：概念节点，包含掌握度、关联概念、误解、信心度等属性
- **KnowledgeGraph 类**：管理完整的知识图谱结构
- **概念自动识别**：从发言中提取物理学科概念
- **误解检测**：自动识别常见学习误解
- **知识缺口分析**：基于掌握度阈值筛选需要加强的概念
- **可视化展示**：生成文本形式的知识图谱可视化
- **JSON 持久化**：支持知识图谱的导入和导出
- **多智能体对比**：支持不同学生知识状态的对比分析

**技术特点：**
- 仅使用 Python 标准库，无需额外依赖
- 兼容 Windows 控制台（UTF-8 编码处理）
- 可独立运行测试，也可集成到 AutoGen 智能体中

**运行方式：**
```bash
python knowledge_graph.py
```

## 构建和运行

### 环境配置
```bash
# 创建虚拟环境
python -m venv .venv

# 激活虚拟环境（Windows）
.venv\Scripts\activate

# 激活虚拟环境（Linux/Mac）
source .venv/bin/activate

# 安装依赖
pip install -r requirements.txt
```

### 环境变量配置
项目使用 `.env` 文件配置 OpenAI API：
```bash
OPENAI_MODEL_NAME=gpt-4
OPENAI_API_KEY=your_api_key
OPENAI_BASE_URL=https://api.openai.com/v1
```

### 运行示例

**中心化群聊模式（推荐）：**
```bash
python test_manager_ver.py
```

**点对点对话模式：**
```bash
python test_p2p_ver.py
```

## 开发约定

### 代码结构
- 所有智能体定义在脚本内部的 `create_heterogeneous_agents()` 函数中
- 配置通过 `config_list` 变量管理，从环境变量读取
- 自定义逻辑通过继承 AutoGen 类实现（如 `EnhancedClassroomChat`）

### 智能体设计原则
- 每个智能体通过 `system_message` 定义其人格、知识水平和语言风格
- 避免在发言开头加前缀（如 ":Teacher"）
- 语言风格应口语化、真实化，符合学生或老师的实际表达
- 回答长度应控制在合理范围（1-3句话）

### 日志和调试
- 使用 `print()` 输出调度日志，格式：`[调度日志] 描述`
- 讨论结束后自动输出统计数据（发言次数、互动次数、占比等）
- 异常消息会被检测并跳过

### AutoGen 配置
- `max_round`：控制讨论最大轮数（默认 25）
- `allow_repeat_speaker`：禁止连续重复发言
- `speaker_selection_method`：使用自定义手动选择
- `temperature`：控制回复的随机性（学生 0.65-0.9，老师 0.5-0.7）

## 与设计文档的差距

根据 `仿真平台后端设计：基础平台与异构智能体.md` 中的规划，当前实现主要在以下方面已验证可行性：

### 已完成
- **对话与交互模块**：验证了 GroupChat 模式和发言时机调控
- **干预模块**：实现了部分行为层干预（发言时机调控）
- **异构智能体**：通过 system_message 实现了不同人格的学生
- **知识状态建模**：实现了动态知识图谱构建与演化模块（2025-12-28）

### 待实现
1. **从脚本到服务**：需要将智能体定义持久化，封装成 API 服务
2. **知识图谱集成**：将 `knowledge_graph.py` 集成到 `test_manager_ver.py` 的智能体中，实现实时更新
3. **数据驱动策略**：引入数据分析与评估，支持策略自动优化
4. **自动干预规则库**：建立可配置的干预规则库，支持自动触发
5. **混合通信模式**：结合群聊和点对点对话
6. **工具使用能力**：让智能体能够使用外部工具（搜索、代码执行等）

## 下一步发展方向

### 基础功能扩展

1. **实现混合通信模式**：通过函数调用将点对点对话作为群聊的工具，支持老师在群聊中发起临时私密对话
2. **构建更复杂的发言人选择策略**：引入"发言意愿"、"对话记忆"等概念，实现更自然的对话流转
3. **赋予智能体使用工具的能力**：注册工具函数，如搜索、代码执行、知识库查询
4. **探索智能体的状态与记忆**：为每个智能体附加状态管理器，记录学习成果和情绪状态
5. **集成知识图谱到智能体**：将 `knowledge_graph.py` 模块集成到 `test_manager_ver.py` 的智能体中，实现讨论过程中的实时知识图谱更新

### 创新方向（重点推荐）

#### 1. 🧠 动态知识图谱构建与演化
**创新点**：让智能体在讨论过程中实时构建和更新个人知识图谱，实现知识状态的显式建模

**实现思路**：
- 为每个智能体维护一个知识图谱，节点代表概念，边代表概念间的关系
- 在对话中实时识别新概念，动态添加到知识图谱中
- 根据讨论内容更新概念间的关联强度和掌握程度
- 支持知识图谱的可视化，展示每个学生的知识结构和学习轨迹

**技术路径**：
```
知识图谱结构:
{
  "concepts": {
    "牛顿第一定律": {
      "mastery": 0.8,
      "connections": ["惯性", "摩擦力", "匀速运动"],
      "last_mentioned": "2025-12-28",
      "confidence": 0.75
    },
    "惯性": {
      "mastery": 0.6,
      "connections": ["牛顿第一定律", "质量"],
      "misconceptions": ["惯性是力"]
    }
  }
}
```

**应用价值**：
- 自动检测学生的知识盲区
- 个性化推荐学习内容
- 量化学习效果评估

---

**✅ 实现状态**：已完成（2025-12-28）

**📁 实现文件**：`knowledge_graph.py`

**📋 已完成工作**：

1. **核心数据结构**
   - `Concept` 类：概念节点，包含掌握度、关联概念、误解、信心度、提及次数等属性
   - `KnowledgeGraph` 类：管理完整的知识图谱结构，支持概念添加、更新、查询

2. **核心功能实现**
   - **概念自动识别**：从发言中提取物理学科概念（预定义概念库 + 正则表达式）
   - **误解检测**：自动识别常见学习误解（如"惯性是力"）
   - **知识缺口分析**：基于掌握度阈值筛选需要加强的概念
   - **知识图谱可视化**：生成文本形式的可视化展示
   - **JSON 持久化**：支持知识图谱的导入和导出
   - **多智能体对比**：支持不同学生知识状态的对比分析

3. **技术特点**
   - 仅使用 Python 标准库（re、json、datetime、typing、collections）
   - 无需额外安装依赖包
   - 兼容 Windows 控制台（UTF-8 编码处理）

4. **测试验证**
   - 单智能体测试：成功处理 7 条讨论发言，识别 12 个概念，检测到误解
   - 多智能体对比测试：对比 3 个学生的知识状态
   - 生成 JSON 导出文件：`student_a_knowledge_graph.json`

5. **运行方式**
   ```bash
   python knowledge_graph.py
   ```

**🔗 集成方向**：
- 将 `KnowledgeGraph` 集成到 `test_manager_ver.py` 的智能体中
- 在讨论过程中实时更新每个智能体的知识图谱
- 根据知识图谱状态动态调整教学策略

---

#### 2. 🎭 多模态情感识别与自适应干预
**创新点**：结合文本、语音（未来）、表情（未来）等多模态数据，识别智能体和用户的情感状态，实现智能化的情感干预

**实现思路**：
- 通过 NLP 技术分析发言内容的情感倾向（积极、消极、焦虑、困惑等）
- 构建情感状态机，跟踪每个智能体的情感变化轨迹
- 当检测到负面情感时，自动触发针对性的干预策略
- 老师智能体可以根据情感分析结果调整教学风格

**情感状态模型**：
```python
class EmotionalState:
    primary_emotion = "neutral"  # 主导情绪
    arousal_level = 0.5          # 激活度
    valence = 0.0                # 情感正负值
    emotion_history = []         # 情感变化历史
    
    def detect_sentiment_shift(self, current_utterance):
        """检测情感突变"""
        pass
    
    def suggest_intervention(self):
        """建议干预措施"""
        if self.arousal_level > 0.8:
            return {"type": "calm_down", "message": "让我们放松一下"}
        elif self.valence < -0.5:
            return {"type": "encourage", "message": "你已经做得很好了"}
```

**干预策略库**：
- 情感支持型：鼓励、共情、幽默化解
- 认知调节型：引导重新思考、提供新视角
- 行为引导型：调整讨论节奏、切换话题

---

#### 3. 🔄 元认知反思机制
**创新点**：让智能体具备"思考自己思考"的能力，能够对讨论过程进行元认知反思，提升学习质量

**实现思路**：
- 在关键节点（如讨论结束、遇到困难）触发元认知反思
- 智能体自我评估：理解程度、参与质量、学习收获
- 生成反思报告，包含：学到了什么、还有什么困惑、如何改进
- 老师智能体根据反思结果提供针对性反馈

**元认知提示框架**：
```
反思维度：
1. 认知层面
   - "我对这个概念的理解程度是？"
   - "我还能举出其他例子吗？"
   - "这个观点和之前的讨论有什么联系？"

2. 元认知层面
   - "我的思考过程是否合理？"
   - "我是否遗漏了什么重要信息？"
   - "下次如何改进？"

3. 社会层面
   - "我是否充分听取了别人的观点？"
   - "我的发言对讨论有贡献吗？"
   - "我们达成了哪些共识？"
```

**实现示例**：
```python
def trigger_metacognitive_reflection(agent, dialogue_context):
    reflection_prompt = f"""
    {agent.name}，请对刚才的讨论进行反思：
    
    1. 你学到了什么新知识？（用关键词总结）
    2. 你还有什么困惑？
    3. 你认为自己在讨论中的表现如何？
    4. 下次如何改进？
    
    请用简洁、真实的方式回答。
    """
    return reflection_prompt
```

---

#### 4. 🎯 个性化学习路径推荐引擎
**创新点**：基于智能体的知识状态、学习风格、兴趣偏好，动态生成个性化学习路径

**实现思路**：
- 构建学习目标图谱，定义知识点之间的依赖关系
- 分析每个智能体的学习风格（视觉型、听觉型、动觉型）
- 根据当前知识掌握程度，推荐最合适的学习内容和活动
- 支持实时调整学习路径，适应学习进度

**学习路径模型**：
```python
class LearningPathGenerator:
    def __init__(self, knowledge_graph, learning_style):
        self.kg = knowledge_graph
        self.style = learning_style
    
    def generate_path(self, current_state, target_goal):
        """
        生成学习路径
        
        参数:
            current_state: 当前知识状态
            target_goal: 目标知识点
        
        返回:
            [
                {"concept": "A", "activity": "讨论", "duration": 10},
                {"concept": "B", "activity": "实验", "duration": 15},
                {"concept": "C", "activity": "反思", "duration": 5}
            ]
        """
        # 分析知识缺口
        gaps = self._analyze_gaps(current_state, target_goal)
        
        # 根据学习风格选择活动类型
        activities = self._map_to_activities(gaps, self.style)
        
        # 优化路径顺序
        path = self._optimize_sequence(activities)
        
        return path
```

**学习风格适配**：
- 视觉型：推荐图表、动画、思维导图
- 听觉型：推荐讲解、讨论、音频材料
- 动觉型：推荐实验、模拟、互动练习

---

#### 5. 🌐 跨文化智能体协作
**创新点**：引入不同文化背景的智能体，模拟跨文化学习环境，培养全球视野

**实现思路**：
- 为智能体赋予不同的文化背景（东方文化、西方文化、混合文化）
- 文化差异体现在：思维方式、表达习惯、价值观、学习风格
- 在讨论中自然展现文化碰撞与融合
- 老师智能体引导跨文化理解和尊重

**文化特征建模**：
```python
class CulturalProfile:
    # 东方文化特征
    eastern = {
        "communication": "含蓄、间接",
        "thinking": "整体性、关联性",
        "values": ["集体主义", "和谐", "尊重权威"],
        "learning_style": "反思型、观察型"
    }
    
    # 西方文化特征
    western = {
        "communication": "直接、明确",
        "thinking": "分析性、逻辑性",
        "values": ["个人主义", "创新", "批判思维"],
        "learning_style": "行动型、实验型"
    }
```

**应用场景**：
- 国际化课堂模拟
- 全球问题讨论（气候变化、可持续发展）
- 文化差异认知训练

---

#### 6. 📊 实时学习分析仪表盘
**创新点**：构建可视化仪表盘，实时展示讨论动态、学习进度、互动网络等关键指标

**实现思路**：
- 实时采集讨论数据（发言、互动、情感、知识状态）
- 多维度可视化：时间线、网络图、热力图、雷达图
- 支持交互式探索，点击节点查看详细信息
- 提供异常检测和智能预警

**可视化指标**：
```
实时仪表盘包含：

1. 讨论动态
   - 发言时间线
   - 话题演化图
   - 活跃度趋势

2. 学习进度
   - 知识掌握热力图
   - 学习路径追踪
   - 目标达成度

3. 互动网络
   - 社交网络图
   - 互动强度矩阵
   - 影响力排名

4. 情感状态
   - 情感时间序列
   - 情感分布图
   - 情感转移网络

5. 异常预警
   - 沉默检测
   - 话题偏离
   - 情绪异常
```

**技术实现**：
- 前端：React + D3.js / ECharts
- 后端：WebSocket 实时推送
- 数据存储：时序数据库（InfluxDB）+ 图数据库（Neo4j）

---

### 优先级建议

**短期（1-2个月）**：
- ✅ ~~实现动态知识图谱构建~~（已完成 2025-12-28）
- 添加基础情感识别功能
- 将知识图谱集成到现有智能体中

**中期（3-6个月）**：
- 开发元认知反思机制
- 构建个性化学习路径推荐

**长期（6-12个月）**：
- 实现跨文化智能体协作
- 完成实时学习分析仪表盘

## 开发历史

### 2025-12-28 第一轮创作：动态知识图谱构建与演化

**目标**：实现第一个创新方向 - 动态知识图谱构建与演化

**工作内容**：

1. **需求分析**
   - 分析现有项目结构和 IFLOW.md 文档
   - 确定动态知识图谱的核心功能需求

2. **设计与实现**
   - 创建 `knowledge_graph.py` 文件
   - 实现 `Concept` 类和 `KnowledgeGraph` 类
   - 实现概念提取、误解检测、知识缺口分析等功能
   - 添加可视化展示和 JSON 持久化支持

3. **问题解决**
   - 解决 Windows PowerShell 命令语法问题（`&&` 改为 `;`）
   - 解决 Windows 控制台 Unicode 编码问题（添加 UTF-8 编码设置）

4. **测试验证**
   - 编写测试用例，验证单智能体和多智能体功能
   - 确认无需额外安装依赖包
   - 成功运行并生成测试结果

**成果**：
- ✅ `knowledge_graph.py` - 功能完整的知识图谱模块
- ✅ `student_a_knowledge_graph.json` - 测试导出的知识图谱数据
- ✅ IFLOW.md 更新 - 记录实现状态和集成方向

**下一步**：
- 将知识图谱集成到 `test_manager_ver.py` 的智能体中
- 实现讨论过程中的实时知识图谱更新

---

## 文档参考

- `README.md`：项目使用说明和当前实现效果
- `仿真平台后端设计：基础平台与异构智能体.md`：完整的后端架构设计文档
- `output1.md`：示例讨论输出和数据分析
- `knowledge_graph.py`：动态知识图谱构建与演化模块（2025-12-28 实现）
- `student_a_knowledge_graph.json`：知识图谱测试导出数据